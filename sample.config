dbconnect = user='smnl' dbname='smnl' application_name='Smnl'

interface = eth0
load_module = modules/sample.so
# module options
test = yup

interface = wlan0
load_module = modules/sample.so
# module options
test = nope
